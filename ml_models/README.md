# ML Models Workspace

This folder is reserved for exported or versioned model artifacts for research experiments.

Current runtime models are implemented under:

- `backend/app/ml_models/`
- `backend/app/behavior_models/`

Use this folder to store model checkpoints for publication reproducibility.
